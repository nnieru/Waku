import LoginForm from "~/features/auth/login/components/LoginForm";

export const metadata = {
  title: "Login | Waku",
  description: "Welcome back to Waku. Let's stack that bread.",
};

export default function LoginPage() {
  return <LoginForm />;
}
